using System;

namespace NeedForSpeed
{
    public class StartUp
    {
        public static void Main(string[] args)
        {

            var hP = int.Parse(Console.ReadLine());
            var fuel = double.Parse(Console.ReadLine());

            var car = new Vehicle(hP, fuel);
            car.Drive(5.0);
        }
    }
}
